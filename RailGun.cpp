#include <iostream>
#include <cmath>
#include <conio.h>
#include "progressbar.hpp"

int main() {

    /*
        Changeable variables
    */

    //Friction coefficients between armature, and rails and plates
    const double mu_s = 0;
    const double mu_k = 0;
    
    //Properties of individual rails
    const double l_r = 0;
    const double w_r = 0;
    const double h_r = 0;
    const double dens_r = 0;
    const double resistiv_r = 0;
    const double SpecHeat_r = 0;

    //Properties of armature
    const double l_a = 0;
    const double w_a = 0;
    const double h_a = 0;
    const double dens_a = 0;
    const double resistiv_a = 0;
    const double SpecHeat_a = 0;
    const double alpha_V = 0;
    
    //Properties of plates
    const double h_pl = 0;
    const double SpecHeat_pl = 0;

    //Properties of power wires
    const double l_pw = 0;
    const double A_pw = 0;
    const double resistiv_pw = 0;

    //Properties of powersupply
    bool ConstVolt = false;
    const double C = 0;
    const double U0 = 0;

    /*
        Calculations based on properties
    */

   //Armature
   

    return 0;
}